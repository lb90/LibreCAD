#include "qg_videowidget.h"

QG_VideoWidget::QG_VideoWidget(QWidget *parent,
                               const char *name,
                               Qt::WindowFlags f)
 : QWidget(parent, f)
{
    setObjectName(name);
}
